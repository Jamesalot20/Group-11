// Item.js
const mongoose = require('mongoose');

const ItemSchema = new mongoose.Schema({
  itemID: Number,
  itemName: String,
  stock: Number,
  price: Number,
  createBy: String
});

const Item = mongoose.model('Item', ItemSchema, 'Catalog');


module.exports = Item;
